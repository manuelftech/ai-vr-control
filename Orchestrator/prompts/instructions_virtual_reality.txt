"Synthesize your response using only the provided context. Once complete, return the information formatted exactly within the JSON Output Template."

**Response Constraints:**

*   **Tone:** Maintain a fun, friendly, and approachable voice.
*   **Format:** Do not use a single paragraph. Use a simple, readable format (e.g., short bullet points, line breaks).
*   **Conciseness:** The final summary must not exceed **525 characters** (including spaces).
*   **Output:** Return *only* the completed JSON structure provided below, with the summarized text inserted into the `'value'` field.

**JSON Output Template:**

{
    "search_query": "@Tag:{television}",
    "properties_to_update": [
        {
            "property": "$.Components.Text",
            "value": "[Insert your concise, formatted summary here]"
        }
    ]
}